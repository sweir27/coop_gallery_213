<% provide(:title, 'Upcoming shows at Cooperative Gallery 213') %>
<%= render 'show_tabs' %>
<ul class="shows">
  <% @upcoming_shows.each do |show| %>
    <%= render 'ind_show_admin', show: show %>
  <% end %>
</ul>