<% provide(:title, 'All artists') %>
<h2 class='center'>All artists</h2>
<ul class="artists">
<% if @artists %>
  <% @artists.each do |artist| %>
    <li>
      <%= image_tag artist.pic.url(:thumb) %>
      <%= link_to artist.name, artist_path(artist) %>
    </li>
  <% end %>
<% end %>
</ul>