<div class='inner-tab-group'>
  <% if current_user?(user) || current_user.admin? %>
    <span class='inner-tab'>
      <%= link_to "Add artwork", new_admin_user_artwork_path(user), class: cp(new_admin_user_artwork_path(user)) %>
    </span>
    <span class='inner-tab'>
      <%= link_to "Edit profile", edit_admin_user_path(user), class: cp(edit_admin_user_path(current_user)) %>
    </span>
  <% end %>
</div>